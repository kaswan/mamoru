<%= link_to _('Back'), profiles_path, {class: 'second'} %>
<div class="main_box2">
  <div class="main_box_in">
    <h1><%= _('Change Profile Information') %></h1>
    <h2 class="title"><%= _('Basic Information') %></h2>
    <%= simple_nested_form_for @user, {:url => "/profiles/#{current_user.id}"} do |f| %>
      <%= render '/shared/users/form', :f => f %>
    <% end %>
  </div>
</div>    
  