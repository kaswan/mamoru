<div class="main_box2">
  <div class="main_box_in">
    <h1><%= _('Listing Tutorials')%></h1>
    <h2><%= link_to _('New Tutorial'), new_admins_tutorial_path %></h2>
    <table class="history">      
      <thead>
        <tr>
          <th><%= _('Title') %></th>
          <th><%= _('Summary') %></th>
          <th><%= _('Is free') %></th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @tutorials.each do |tutorial| %>
          <tr>
            <td><%= tutorial.title %></td>
            <td><%= tutorial.summary %></td>
            <td><%= tutorial.is_free %></td>
            <td><%= link_to _('Show'), [:admins, tutorial] %></td>
            <td><%= link_to _('Edit'), edit_admins_tutorial_path(tutorial) %></td>
            <td><%= link_to _('Destroy'), [:admins, tutorial], method: :delete, data: { confirm: _('Are you sure?') } %></td>
          </tr>
        <% end %>
      </tbody>
      
      <thead>
        <tr>
          <th><%= _('Title') %></th>
          <th><%= _('Summary') %></th>
          <th><%= _('Is free') %></th>
          <th colspan="3"></th>
        </tr>
      </thead>
    </table>
  </div>
</div>
